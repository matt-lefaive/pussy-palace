import React from 'react';

const FloorMap = ({ room }) => {
    return (
        <div id='floor-map' className='floor-map hidden'>
            MAP TEST
        </div>
    );
}

export default FloorMap;